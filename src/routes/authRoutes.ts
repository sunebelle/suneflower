import * as _ from "lodash";
import { useMemo } from "react";
import { pathHome } from "../constants/path";

export const PublicRoutes = () => [
  
  {
    path: pathHome.FORGOT_PASSWORD,
    exact: true,
    // component: ,
    permission:""
  },
];


 
